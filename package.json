{
  "name": "ezlog",
  "version": "0.0.13",

  "description": "Simple logger function that takes care of prefixes and colored output.",

  "scripts": {
    "test": "node node_modules/istanbul/lib/cli.js cover node_modules/mocha/bin/_mocha \"./test/mocha/*.js\"",
    "localTest": "node node_modules/istanbul/lib/cli.js cover node_modules/mocha/bin/_mocha \"./test/mocha/*.js\" && npm run coverall",
    "coveralls": "node node_modules/coveralls/bin/coveralls.js < coverage/lcov.info"
  },

  "dependencies": {
    "opensoars_cls": "0.0.8"
  },

  "devDependencies": {
    "mocha": "*",
    "istanbul": "*",
    "coveralls": "*"
  },

  "repository": {
    "type": "git",
    "url": "https://github.com/opensoars/ezlog"
  },

  "readmeFilename": "README.md"
}
