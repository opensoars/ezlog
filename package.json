{
  "name": "ezlog",
  "version": "0.0.6",
  "scripts": {
    "test": "mocha ./test/mocha.js && npm run coverage",
    "coverage": "istanbul cover ./test/coverage.js",
    "coveralls": "npm coverage && node bin/coveralls.js < coverage/lcov.info"
  },
  "dependencies": {
    "opensoars_cls": "*",
    "coveralls": "~2.11.1"
  },  

  "devDependencies": {
    "mocha": "*",
    "istanbul": "*",
    "coveralls": "*"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/opensoars/ezlog"
  },
  "readmeFilename": "readme.md"
}
